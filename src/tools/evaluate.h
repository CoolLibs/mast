#pragma once

#include <cmath>
#include <string>
#include "../TreeNode.h"

namespace mast {

// ToDo : Do we keep that in the final project ?
auto evaluateAST(TreeNode const& tree) -> double;

} // namespace mast
